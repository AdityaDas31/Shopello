import React from 'react';
import { Avatar, Box, Button, Menu, MenuButton, MenuDivider, MenuItem, MenuList, Select, Text, Tooltip, WrapItem } from '@chakra-ui/react';
import { ChevronDownIcon, BellIcon } from '@chakra-ui/icons';

const navbar = () => {
    return (
        <>
        navbar
        </>
    )
}

export default navbar;